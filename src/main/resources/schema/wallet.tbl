CREATE TABLE wallet (

	id BIGINT NOT NULL AUTO_INCREMENT,

	name VARCHAR(255) NOT NULL,
	hot_wallet_id VARCHAR(255),
    created_at TIMESTAMP NOT NULL,

    CONSTRAINT wallet_pk PRIMARY KEY (id),
    CONSTRAINT wallet_name_uq UNIQUE (name)
);